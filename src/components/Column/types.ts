export interface columnProps {
    headerColor: string;
    title: string;
    cards?: any;
}

export interface taskCardProps{
    id: number;
    tag: string;
    descricao: string;
    responsavel: string;
    upload?: any;
}